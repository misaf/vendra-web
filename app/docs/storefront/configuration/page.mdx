---
description: "The required storefront property configuration shape and the complete runtime environment contract."
---

import { Callout } from 'nextra/components'

# Property configuration

<Lede>
  Production injects a base64-encoded JSON object as `STOREFRONT_CONFIG_BASE64`.
  The readiness endpoint rejects a production container without a valid one.
</Lede>

<Callout type="error">
  The refusal is deliberate. Falling back to the bundled example configuration
  could serve another tenant’s branding on a live domain.
</Callout>

## Required shape

```json filename="property.json"
{
  "slug": "acme",
  "theme": "default",
  "domain": "shop.example.com",
  "siteUrl": "https://shop.example.com",
  "name": { "en": "Acme", "fa": "آکمه" },
  "businessType": "Florist",
  "priceCurrency": "IRR",
  "address": { "locality": "Tehran", "country": "IR" },
  "contact": {
    "mobilePhone": "09120000000",
    "officePhone": "02100000000",
    "email": "hello@example.com",
    "hoursOpen": "08:00",
    "hoursClose": "21:00",
    "mapQuery": "35.7,51.4"
  },
  "social": {
    "whatsappPhone": "+989120000000",
    "telegramUsername": "acme",
    "instagramUsername": "acme"
  }
}
```

{/* `scripts/check-drift.mjs` reads the optional field names out of the
    paragraph below. It anchors on that opening phrase at the start of a line,
    and on the backticks. Keep both when rewording, or the check will report
    the whole list as undocumented. */}

Optional fields include `ogImage`, `heroImage`, `aboutImage`, `messages`,
`checkout`, `currency`, and `trustSeal`. Runtime messages are deep-merged over
the brand-neutral base catalog, per locale.

Generate a configuration with:

```bash
npm run property:config <slug> -- --json
```

## Environment contract

<DefList
  items={[
    {
      term: 'VENDRA_API_URL',
      tag: { tone: 'emerald', label: 'Required' },
      meta: 'Server runtime',
      description: 'Canonical API origin. The /api suffix is normalized.'
    },
    {
      term: 'STOREFRONT_CONFIG_BASE64',
      tag: { tone: 'emerald', label: 'Required' },
      meta: 'Server runtime',
      description: 'Complete property identity and presentation configuration.'
    },
    {
      term: 'STORAGE_BASE_URL',
      meta: 'Server runtime, optional',
      description: 'Media origin override.'
    },
    {
      term: 'STOREFRONT_SITE_URL → SITE_URL',
      meta: 'Controller input → server runtime, optional',
      description:
        'Canonical-site override for staging. Unset, the configuration siteUrl applies.'
    },
    {
      term: 'CONTACT_*',
      meta: 'Server runtime, optional',
      description: 'Deployment-specific contact overrides.'
    },
    {
      term: 'NESHAN_SERVICE_KEY',
      meta: 'Server runtime, optional',
      description: 'Reverse-geocoding service key.'
    },
    {
      term: 'VENDRA_STOREFRONT_KEY',
      meta: 'Server runtime, optional',
      description: 'Opaque storefront credential sent as X-Storefront-Key.'
    },
    {
      term: 'VENDRA_STOREFRONT_KEY_HEADER',
      meta: 'Server runtime, optional',
      description:
        'Header name for the credential. Defaults to X-Storefront-Key.'
    },
    {
      term: 'NEXT_PUBLIC_MAP_*',
      tag: { tone: 'amber', label: 'Build time' },
      meta: 'Baked into the image',
      description: 'Fleet-wide browser map configuration.'
    }
  ]}
/>

The platform Console and Reseller forms currently produce the required fields
and optional `ogImage`. The storefront schema also accepts the optional fields
listed above, but `heroImage`, `aboutImage`, `checkout`, `currency`, and
`trustSeal` currently require a manual or programmatic configuration producer.
`storefront_messages` is accepted by the platform deployment action, but no
panel field edits it yet.

<Callout type="error">
  Never place secrets in `NEXT_PUBLIC_*`. Next.js inlines those values into
  client assets, where anyone can read them.
</Callout>

<NextSteps
  items={[
    {
      href: '/docs/storefront/deployment',
      title: 'Build and deployment',
      description: 'How this configuration reaches a running container.'
    },
    {
      href: '/docs/api/storefront',
      title: 'Storefront API and tenancy',
      description: 'What the credential is for, and why it is not yet verified.'
    }
  ]}
/>
