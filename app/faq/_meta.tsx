/**
 * Only the index is listed. Questions are discovered from the page map and
 * ordered by the date they were answered, so adding one never means editing
 * this file.
 */
export default {
  index: 'All questions'
}
