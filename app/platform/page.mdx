---
description: "The Laravel 13 platform is Vendra's business control plane: a thin composition root over first-party packages handling tenants, billing, content, and the public API."
---

# Laravel platform

<Lede>
  `vendra` is a Laravel 13 application and Composer monorepo. It is the business
  control plane: tenants, resellers, subscriptions, content, catalog data, users,
  public APIs, and storefront deployment records all live here.
</Lede>

<ChipRow items={['Laravel 13', 'Filament 5', 'Livewire 4', 'API Platform', 'Horizon']} />

## Composition-root design

The host application is deliberately thin. First-party packages define domain
models, contracts, actions, events, and Filament resources. The host connects
those packages to concrete panels, tenant resolution, reseller billing, wallet
transactions, notifications, and queued provisioning.

<Boundary
  doTitle="Packages own"
  dontTitle="The host owns"
  dos={[
    'Domain models and contracts',
    'Actions and events',
    'Filament resources'
  ]}
  donts={[
    'Concrete tenant and reseller bindings',
    'Panel registration and notification channels',
    'Queued provisioning wiring'
  ]}
/>

## Main workflows

<DefList
  items={[
    {
      term: 'Tenant provisioning',
      description:
        'Creates an inactive tenant, domain, owner, and role, then completes setup asynchronously before marking it ready.'
    },
    {
      term: 'Subscription charging',
      description:
        'Uses durable payment rows, idempotency keys, queued retries, and reconciliation states.'
    },
    {
      term: 'Storefront provisioning',
      description:
        'Persists desired state before calling the controller, so a failed call never loses intent.'
    },
    {
      term: 'Public API',
      description:
        'API Platform resources expose package-owned JSON:API endpoints below /api, documented at /api/docs.'
    }
  ]}
/>

## Operational processes

The controller’s production platform Compose project runs Laravel/FrankenPHP,
MySQL, Redis, Horizon, the scheduler, Pulse ingestion, and the provisioner.

<Flow
  label="Platform Compose project"
  nodes={['FrankenPHP', 'MySQL', 'Redis', 'Horizon', 'Scheduler', 'Pulse', 'Provisioner']}
/>

Laravel application containers do not receive the Docker socket.

## Explore the platform

<FeatureGrid
  cols={3}
  items={[
    {
      href: '/platform/tenancy',
      title: 'Panels and tenancy',
      description:
        'The three Filament panels and how tenants are resolved on each.'
    },
    {
      href: '/platform/packages',
      title: 'Package catalog',
      description: 'What lives in the first-party package monorepo.'
    },
    {
      href: '/platform/development',
      title: 'Development and testing',
      description: 'Setup, checks, static analysis, and common resets.'
    }
  ]}
/>
