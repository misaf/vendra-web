# dependencies
node_modules

# Next.js
.next
out

# production
build

# environment
.env
.env.*
!.env.example

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# system
.DS_Store

# Generated search index. The build writes it to out/ (ignored above) and
# `scripts/sync-search-index.mjs` copies it here so `next dev` can serve it.
public/_pagefind
