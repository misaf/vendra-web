# dependencies
node_modules

# Next.js
.next
out

# production
build

# environment
.env
.env.*
!.env.example

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# system
.DS_Store

# generated search index — now written into out/, which is ignored above.
# Kept so a stale index from an earlier build layout stays out of commits.
public/_pagefind
